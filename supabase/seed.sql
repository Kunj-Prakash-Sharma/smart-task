-- Optional local-dev seed data.
-- Run automatically by `supabase db reset` when using the Supabase CLI locally.
-- Requires at least one auth.users row to exist already (sign up once via the app,
-- then replace the placeholder UUID below with that user's id before seeding lists/tasks).

-- Example (uncomment and fill in a real user id from auth.users after signing up locally):

-- insert into public.lists (name, color, owner_id) values
--   ('Personal', '#6366F1', '00000000-0000-0000-0000-000000000000'),
--   ('Work', '#22C55E', '00000000-0000-0000-0000-000000000000');
